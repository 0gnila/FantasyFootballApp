# CSE434

This project is a fantasy football app that utilizes AI to help with descisions related to fantasy football.

## Setup
Run `pip install -r requirements.txt` to install the required Python Packages.

Run `python -m uvicorn main:app --reload` to run on `127.0.0.1:8000`.